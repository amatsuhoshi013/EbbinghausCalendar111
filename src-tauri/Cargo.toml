[package]
name = "ebbinghaus-calendar"
version = "0.3.0"
description = "艾宾浩斯日程表 - 本地日历与复习计划"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
